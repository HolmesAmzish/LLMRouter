# LLM Router Admin

Vite + React + TypeScript + Tailwind admin console.

```bash
npm install
npm run dev
npm run build
```

Set backend configuration through `backend/etc/dev.env` or `backend/etc/prod.env`.
