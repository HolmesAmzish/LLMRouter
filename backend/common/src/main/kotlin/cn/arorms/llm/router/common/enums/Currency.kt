package cn.arorms.llm.router.common.enums

/**
 * Billing currency. USD is the only supported value for now; keeping this as
 * an enum leaves the model-pricing API ready for future currencies.
 */
enum class Currency {
    USD
}
